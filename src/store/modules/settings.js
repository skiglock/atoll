const state = () => ({
  allSettings: {}
})

const getters = {}

const actions = {}

const mutations = {
  setSettings(state, settings) {
    state.allSettings = settings
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
