const state = () => ({
  windowHeight: null
})

const getters = {}

const actions = {}

const mutations = {
  setWindowHeight(state, height) {
    state.windowHeight = height
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
